<?php
/**
 * Payment step — method list + place order button.
 *
 * @var \Magento\Framework\View\Element\Template $block
 * @var \Magento\Framework\Escaper $escaper
 */
declare(strict_types=1);
?>

<div>

    <!-- Express rail (PSP modules inject here, e.g. Revolut Fast Checkout) -->
    <?= $block->getChildHtml('checkout.express.rail.payment') ?>

    <!-- Method list -->
    <div class="space-y-2 mb-6">
        <?php /* Methods come from $wire->methods — rendered server-side */ ?>
    </div>

    <!-- Place order button -->
    <button type="button"
            wire:click="placeOrder"
            wire:loading.attr="disabled"
            wire:target="placeOrder"
            class="w-full bg-blue-600 text-white font-semibold py-3.5 px-6 rounded-xl hover:bg-blue-700 active:bg-blue-800 transition-colors disabled:opacity-60 disabled:cursor-not-allowed text-base">
        <span wire:loading.remove wire:target="placeOrder">
            <?= $escaper->escapeHtml(__('Place order')) ?>
        </span>
        <span wire:loading wire:target="placeOrder" class="flex items-center justify-center gap-2">
            <svg class="animate-spin w-5 h-5" xmlns="http://www.w3.org/2000/svg" fill="none" viewBox="0 0 24 24">
                <circle class="opacity-25" cx="12" cy="12" r="10" stroke="currentColor" stroke-width="4"></circle>
                <path class="opacity-75" fill="currentColor" d="M4 12a8 8 0 018-8V0C5.373 0 0 5.373 0 12h4z"></path>
            </svg>
            <?= $escaper->escapeHtml(__('Placing order...')) ?>
        </span>
    </button>

    <p class="mt-3 text-center text-xs text-gray-400">
        <?= $escaper->escapeHtml(__('By placing your order you agree to our terms and conditions.')) ?>
    </p>

</div>
