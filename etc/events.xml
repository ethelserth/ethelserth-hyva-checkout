<?xml version="1.0"?>
<!--
    `sales_model_service_quote_submit_before` fires inside
    `Magento\Quote\Model\QuoteManagement::submit` between data assembly
    and order persistence. A `setData` on the order at this point lands
    in the same INSERT — no second UPDATE round-trip, no risk of half-
    written rows.
-->
<config xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:noNamespaceSchemaLocation="urn:magento:framework:Event/etc/events.xsd">

    <event name="sales_model_service_quote_submit_before">
        <observer name="ethelserth_checkout_copy_order_comments"
                  instance="Ethelserth\Checkout\Observer\CopyQuoteOrderComments"/>
    </event>

</config>
